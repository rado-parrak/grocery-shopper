---
phase: 01-foundation-shared-spine
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spikes/artifact-storage-spike.md
  - project-knowledge/writable-state-decision.md
  - project-knowledge/household-ruleset.md
  - project-knowledge/budget.md
autonomous: false
requirements: [FOUND-09]
user_setup:
  - service: claude-artifacts
    why: "The artifact-storage spike needs a published artifact opened on two physical devices signed into the one shared Claude account"
    dashboard_config:
      - task: "Publish a small test artifact and open it on both phones under the same shared account"
        location: "claude.ai → artifact → Publish"
estimate:
  tokens: 30000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A dated go/no-go note records whether agent-writable state persists cross-device, gating Phase 4 learned-state features (FOUND-09, D-08)."
    - "The spike runs Step 0 first — does a storage capability/API exist at all in the account's runtime — and records NO-GO fast if it does not, rather than assuming the community-described API exists (RESEARCH Pitfall 1)."
    - "On NO-GO or INCOMPLETE, writable-state-decision.md names the fallback explicitly: Rohlík-native favourites/order-history + hand-edited Project-file diffs."
    - "The household's ⚠ FILL placeholders (allergies, dislikes/never-buy, brand preferences, budget soft/hard amounts) are filled, or explicitly acknowledged as must-fill-before-first-real-shop."
  artifacts:
    - spikes/artifact-storage-spike.md
    - project-knowledge/writable-state-decision.md
    - project-knowledge/household-ruleset.md
    - project-knowledge/budget.md
  key_links:
    - "writable-state-decision.md is the one-way gate Phase 4 (staples cadence, preferences) binds to; switching stores later means migrating persisted learned state (D-08)."
    - "Filled hard constraints in household-ruleset.md are what the resolution cascade filters on before every real shop."
---

<objective>
Settle the single biggest open question in the stack — whether agent-writable state can persist across
the household's two devices — and personalise the read-only config. Author the storage-spike protocol
(with the Step-0 existence check this research added), gate the actual spike behind a human checkpoint
run on two physical phones, then record a dated go/no-go that Phase 4 binds to; alongside, capture the
household's private ⚠ FILL values into the ruleset and budget.

Purpose: This session's live capability roster lists only `downloads` and `mcp` — no `storage` — so the
community-described storage API cannot be assumed to exist. Step 0 makes "does it exist at all right now"
the literal first check, turning a NO-GO into a fast, cheap, valid outcome instead of wasted effort (D-08,
RESEARCH Pitfall 1). Output: spike protocol + dated go/no-go note + personalised ruleset/budget.
</objective>

<execution_context>
@/home/user/grocery-shopper/.claude/gsd-core/workflows/execute-plan.md
@/home/user/grocery-shopper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shared-spine/01-CONTEXT.md
@.planning/phases/01-foundation-shared-spine/01-RESEARCH.md
@.planning/phases/01-foundation-shared-spine/SKELETON.md
@project-knowledge/household-ruleset.md
@project-knowledge/budget.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the artifact-storage spike protocol and the go/no-go decision scaffold</name>
  <files>spikes/artifact-storage-spike.md, project-knowledge/writable-state-decision.md</files>
  <read_first>
    01-RESEARCH.md §"Artifact-Storage Spike Protocol" (Step 0 existence check, then write-from-Device-A,
    read-from-Device-B, record go/no-go) and §"Open Questions" 1; 01-CONTEXT.md D-08; §"Environment
    Availability" (two-devices-one-account requirement, and the single-device incomplete case).
  </read_first>
  <action>
    Author two docs.
    (1) artifact-storage-spike.md: the exact human-executed protocol. Step 0 first — in the shared
    Project, create and publish a small test artifact and check whether a persistent-storage API exists
    at all in the runtime (check both reported names for the global), noting explicitly if the artifact
    tool's capability picker only offers downloads/mcp; if neither storage API exists, the protocol says
    record NO-GO immediately and stop. Only if one exists: Step 1 — from Device A, through the normal
    chat-driven flow, have the artifact write a test key/value, recording the literal call made; Step 2 —
    on Device B (second phone, same shared account, fresh session), open the same published artifact URL
    and attempt to read the key back, recording whether it returned and any delay; Step 3 — record the
    result. Note the two-physical-devices-on-one-shared-account precondition, and that a single-device run
    can only test same-device write/read and must be flagged INCOMPLETE (not NO-GO), because it does not
    answer the cross-device question. State the step is human-executed and not runnable from this repo/CI.
    (2) writable-state-decision.md: a scaffold with a dated decision line and three outcome branches to be
    filled — GO (artifact storage becomes the home for Phase 4 learned state; name which pool was used),
    NO-GO (fallback = Rohlik-native favourites/order-history as primary + hand-edited Project-file diffs
    proposed by a future household-prefs skill), and INCOMPLETE (what was missing; default to the NO-GO
    fallback until re-run). State that this decision is one-way for Phase 4 dependents.
  </action>
  <verify>
    <automated>test -f spikes/artifact-storage-spike.md && test -f project-knowledge/writable-state-decision.md && grep -qi "step 0\|existence" spikes/artifact-storage-spike.md && grep -qi "device b\|second" spikes/artifact-storage-spike.md && grep -qi "incomplete" spikes/artifact-storage-spike.md && grep -qi "go/no-go\|no-go\|fallback" project-knowledge/writable-state-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - artifact-storage-spike.md leads with a Step-0 existence check that permits a fast NO-GO, then cross-device write(A)/read(B), and flags the single-device case INCOMPLETE.
    - writable-state-decision.md scaffolds GO / NO-GO / INCOMPLETE branches with the named fallback and a one-way-for-dependents note.
    - Both state the spike is human-executed, not runnable from this repo/CI.
  </acceptance_criteria>
  <done>artifact-storage-spike.md (Step-0 existence check → cross-device write/read, single-device INCOMPLETE flag) and writable-state-decision.md (GO/NO-GO/INCOMPLETE scaffold with named fallback + one-way note) exist and state the spike is human-executed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human — run the storage spike on two devices and provide the ⚠ FILL values</name>
  <what-built>
    artifact-storage-spike.md (Step-0 existence check → cross-device write/read) and the ⚠ FILL
    placeholders standing in household-ruleset.md and budget.md.
  </what-built>
  <how-to-verify>
    Two private household activities:
    (A) Storage spike — on the shared Claude account: publish a small test artifact and check whether a
        persistent-storage API exists (Step 0). If it does not, note that (fast NO-GO). If it does, write
        a test key from Device A, then on Device B (second phone, same account, fresh session) open the
        same published artifact URL and try to read it back; note whether it returned and any delay. If
        only one device is available, run same-device write/read and mark it INCOMPLETE.
    (B) Personalise — paste your real values for the ⚠ FILL fields: allergies, dislikes/never-buy, brand
        preferences (household-ruleset.md §A/§B), and your soft/hard budget amounts in CZK (budget.md);
        or reply "fill later" to defer them to before the first real shop.
  </how-to-verify>
  <resume-signal>
    Paste (A) the storage-spike outcome — "storage API missing" / the cross-device read result / "one
    device only" — and (B) your ⚠ FILL values, or "fill later".
  </resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm the writable-state binding for Phase 4 (one-way door)</name>
  <decision>Which store Phase 4 learned-state (staples cadence, preferences, repertoire) binds to, based on the spike outcome.</decision>
  <context>This is one-way for dependents: switching stores later means migrating persisted learned state (D-08). The spike result dictates the recommended branch; confirm before it is committed to writable-state-decision.md.</context>
  <options>
    <option id="go-artifact-storage">
      <name>GO — artifact storage</name>
      <pros>Skill-writable cross-device store; no extra infrastructure; supports metadata Rohlík can't model (restock cadence, rejected-substitution history).</pros>
      <cons>Only valid if the spike recorded a real cross-device read-back; unpublishing deletes all storage.</cons>
    </option>
    <option id="nogo-rohlik-native">
      <name>NO-GO — Rohlík-native + hand-edited Project files</name>
      <pros>Zero unverified dependencies; uses durable retailer account state; safe default.</pros>
      <cons>Can't hold household metadata Rohlík has no field for; preference edits surface as hand-edited diffs, not transparent writes.</cons>
    </option>
    <option id="incomplete-defer">
      <name>INCOMPLETE — default to the NO-GO fallback until re-run</name>
      <pros>Honest when only one device was available; keeps Phase 4 unblocked on the safe fallback.</pros>
      <cons>The cross-device question stays open; must be re-run before any artifact-storage-dependent feature.</cons>
    </option>
  </options>
  <resume-signal>Select: go-artifact-storage, nogo-rohlik-native, or incomplete-defer (must match the spike result — GO requires a recorded cross-device read-back).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the writable-state go/no-go and apply the household's private values</name>
  <files>project-knowledge/writable-state-decision.md, project-knowledge/household-ruleset.md, project-knowledge/budget.md</files>
  <read_first>
    The human's pasted spike outcome and ⚠ FILL values from the checkpoint; artifact-storage-spike.md;
    writable-state-decision.md scaffold (Task 1); 01-CONTEXT.md D-02/D-03/D-04.
  </read_first>
  <reversibility rating="one-way">writable-state-decision.md binds Phase 4's learned-state store; the human checkpoint is its decision gate. Switching later means migrating persisted state (D-08).</reversibility>
  <action>
    Complete writable-state-decision.md by selecting and dating the branch the spike outcome dictates —
    GO (name the pool used and that artifact storage is the Phase 4 learned-state home), NO-GO (Rohlik-
    native favourites/order-history + hand-edited Project-file diffs), or INCOMPLETE (what was missing,
    defaulting to the NO-GO fallback until re-run). Do not assert GO without a recorded cross-device
    read-back. Then apply the household's pasted ⚠ FILL values: replace the allergy/dislike/brand-pref
    placeholders in household-ruleset.md §A/§B and the soft/hard amounts in budget.md, preserving the
    section structure and — per D-03 — keeping allergies as "none recorded — unconfirmed" only if the
    household explicitly reported no allergies, never as an inferred absence. If the household replied
    "fill later", leave the placeholders in place and add a dated note in each file that they remain
    must-fill-before-first-real-shop, and record that deferral in the phase summary. Also note in both
    files that the uploaded Project-Knowledge copies must be re-uploaded to reflect these edits.
  </action>
  <verify>
    <automated>grep -qi "GO\|NO-GO\|INCOMPLETE" project-knowledge/writable-state-decision.md && grep -qi "2026" project-knowledge/writable-state-decision.md && test -f project-knowledge/household-ruleset.md && test -f project-knowledge/budget.md</automated>
  </verify>
  <acceptance_criteria>
    - writable-state-decision.md records a dated GO / NO-GO / INCOMPLETE outcome consistent with the spike result; GO appears only if a cross-device read-back was recorded.
    - The ⚠ FILL fields in household-ruleset.md and budget.md are either filled with the household's values (structure preserved; allergy default handled per D-03) or explicitly left with a dated must-fill-before-first-real-shop note.
    - Both config files note that the uploaded Project copies must be refreshed.
  </acceptance_criteria>
  <done>writable-state-decision.md records the dated, spike-consistent, human-confirmed binding (GO only with a recorded cross-device read-back); household-ruleset.md and budget.md are personalised or carry a dated must-fill-before-first-real-shop note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Community-described storage API → Phase 4 design | Assuming an unverified API exists would silently break persistence |
| Household private data → Project files | Real allergies/dislikes cross into durable config; safety-critical for the cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Repudiation / wrong-assumption | writable-state-decision.md | high | mitigate | Step-0 existence check + "no GO without a recorded cross-device read-back" prevent designing Phase 4 on a nonexistent API (Pitfall 1); INCOMPLETE defaults to the safe fallback |
| T-01-09 | Tampering (safety invariant) | household-ruleset.md §A | high | mitigate | Allergy default stays "none recorded — unconfirmed" unless the household explicitly reports none; hard constraints keep §A/§B typed separation (D-03) |
| T-01-10 | Information Disclosure | published test artifact | low | accept | The spike writes only a throwaway test key on a private single-household account; no real household data is written to the test artifact |
</threat_model>

<verification>
- artifact-storage-spike.md and writable-state-decision.md exist; the decision doc records a dated, spike-consistent outcome.
- household-ruleset.md and budget.md are either personalised or carry a dated must-fill-before-first-shop note.
- No GO is recorded without a documented cross-device read-back.
</verification>

<success_criteria>
- FOUND-09 is complete: a documented go/no-go on cross-device writable state gates Phase 4.
- ROADMAP success criterion 4 is satisfied; criteria 2 and 3 are closed out with the household's real (or explicitly deferred) values.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shared-spine/01-03-SUMMARY.md` when done.
</output>
