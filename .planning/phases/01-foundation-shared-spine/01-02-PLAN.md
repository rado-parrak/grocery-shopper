---
phase: 01-foundation-shared-spine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spikes/project-setup-checklist.md
  - spikes/mcp-round-trip-protocol.md
  - spikes/mcp-round-trip-results.md
  - project-knowledge/mcp-degradation.md
autonomous: false
requirements: [FOUND-04]
user_setup:
  - service: rohlik-mcp
    why: "Sole integration surface into the shared Rohlík basket; its real tool surface can only be learned by a live round-trip"
    dashboard_config:
      - task: "Attach the Rohlík MCP custom connector (OAuth) to the shared Project and confirm it shows Connected"
        location: "claude.ai → the shared Project → Connectors / Settings"
      - task: "Upload the eight project-knowledge/ docs as Project Knowledge files"
        location: "claude.ai → the shared Project → Knowledge"
estimate:
  tokens: 30000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "mcp-degradation.md's Observed tool surface section is filled from a real, dated live round-trip — the actual tool names, parameters, return shapes, and at least one real error shape (FOUND-04, D-09)."
    - "The recorded round-trip proves read-before-write + read-back-after-write behaviour empirically: the probe add is only believed present after a cart_read, and the cart is restored to baseline by a reversible remove (safety invariants: idempotent adds, no hallucinated success)."
    - "The eight docs are uploaded to the shared Project and the Rohlík MCP OAuth connector shows Connected (the docs are live where every chat preloads them)."
    - "No OAuth token or credential-shaped string appears anywhere in the recorded results or mcp-degradation.md (V2 Authentication control)."
  artifacts:
    - spikes/project-setup-checklist.md
    - spikes/mcp-round-trip-protocol.md
    - spikes/mcp-round-trip-results.md
    - project-knowledge/mcp-degradation.md
  key_links:
    - "The observed tool names/params recorded here are what every Phase-2+ mutating skill will call — mcp-degradation.md becomes the empirical ground truth replacing the community-tool-list hypothesis."
---

<objective>
Ground the MCP-degradation policy in observed reality. Author a precise, copy-pasteable probe protocol
and a results template, gate the go-live setup (upload docs + attach the OAuth connector) behind a
human checkpoint, then have the household execute one live reversible Rohlík MCP round-trip and paste
the raw observations back — from which the "Observed tool surface" section of mcp-degradation.md is
filled with real tool names, parameters, return shapes, and at least one real error shape.

Purpose: Official Rohlík MCP docs are inaccessible (403) and the only detailed community tool list
belongs to a different, unofficial, differently-authenticated project — so the real tool surface can
only be learned empirically (D-09). Until this runs, mcp-degradation.md is a policy without ground truth.
Output: setup checklist + probe protocol + filled results doc + populated mcp-degradation.md table.
</objective>

<execution_context>
@/home/user/grocery-shopper/.claude/gsd-core/workflows/execute-plan.md
@/home/user/grocery-shopper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shared-spine/01-CONTEXT.md
@.planning/phases/01-foundation-shared-spine/01-RESEARCH.md
@.planning/phases/01-foundation-shared-spine/SKELETON.md
@project-knowledge/mcp-degradation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the go-live setup checklist and the MCP round-trip probe protocol</name>
  <files>spikes/project-setup-checklist.md, spikes/mcp-round-trip-protocol.md</files>
  <read_first>
    01-RESEARCH.md §"Rohlík MCP Empirical Discovery Protocol" (the exact 8-step probe sequence and the
    recording-table headers) and §"Environment Availability" (the pre-flight checklist); 01-CONTEXT.md
    D-09; CLAUDE.md §6 (OAuth-only, credential-scan constraint).
  </read_first>
  <action>
    Author two operational docs the household will follow by hand.
    (1) project-setup-checklist.md: a numbered pre-flight the human completes inside claude.ai — confirm
    the plan tier supports custom Skills + publishable Artifacts; upload the eight project-knowledge/
    docs as Project Knowledge files; attach the Rohlik MCP custom connector via its OAuth flow at
    "This project" scope and confirm it reads Connected; and a final credential-scan reminder to ensure
    no token-shaped string was pasted into any Project file. Each item has an explicit done-state the
    human can tick.
    (2) mcp-round-trip-protocol.md: the exact probe sequence from RESEARCH §"Empirical Discovery
    Protocol", copy-pasteable — product search for a cheap easily-reverted item; cart_read baseline;
    reversible add of exactly one unit; cart_read to verify the add is really present; reversible remove
    of the same item; cart_read to confirm the cart is back to baseline; deliberately force at least one
    error case (invalid/expired product id or malformed param) and record its exact shape; and enumerate
    available tools to check specifically for a favourites-equivalent tool. For each step, tell the human
    exactly what to record: the tool name as actually invoked, the parameters actually passed, the return
    shape (product-id field name, price field, unit/pack-size field, stock field), and — for the write —
    that the return value must not be trusted, only the subsequent cart_read. Include the recording-table
    headers verbatim and a provenance rule that every recorded row is dated and marked observed-live.
    State plainly that this step is human-executed inside a real claude.ai chat and cannot be run from
    this repo or CI.
  </action>
  <verify>
    <automated>test -f spikes/project-setup-checklist.md && test -f spikes/mcp-round-trip-protocol.md && grep -qi "connected" spikes/project-setup-checklist.md && grep -qi "credential\|token" spikes/project-setup-checklist.md && grep -qi "cart_read\|cart read" spikes/mcp-round-trip-protocol.md && grep -qi "error" spikes/mcp-round-trip-protocol.md && grep -qi "favourite\|frequent" spikes/mcp-round-trip-protocol.md</automated>
  </verify>
  <acceptance_criteria>
    - project-setup-checklist.md lists tier check, docs upload, OAuth connector attach + Connected confirmation, and a credential-scan step, each with a tickable done-state.
    - mcp-round-trip-protocol.md contains the full reversible probe sequence, a forced-error step, a favourites-tool check, per-step recording instructions, the recording-table headers, and a dated observed-live provenance rule.
    - Both docs state the round-trip is human-executed and not runnable from this repo/CI.
  </acceptance_criteria>
  <done>project-setup-checklist.md and mcp-round-trip-protocol.md exist, copy-pasteable, covering upload + OAuth-connect + Connected confirmation + credential scan, and the full reversible probe sequence with recording headers and a forced-error and favourites-tool check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human — upload docs, attach the OAuth connector, run the live MCP round-trip</name>
  <what-built>
    project-setup-checklist.md and mcp-round-trip-protocol.md — the exact steps to upload the eight docs,
    attach the Rohlík MCP OAuth connector, and run one live reversible cart round-trip.
  </what-built>
  <how-to-verify>
    On a phone signed into the shared Claude account, in the shared Project:
    1. Work through project-setup-checklist.md: upload the eight project-knowledge/ docs; attach the
       Rohlík MCP connector via OAuth; confirm it shows "Connected".
    2. Work through mcp-round-trip-protocol.md end to end: search → cart_read → reversible add → cart_read
       (verify present) → reversible remove → cart_read (verify back to baseline) → force one error →
       check for a favourites tool.
    3. Paste back, for each step: the exact tool name invoked, the parameters passed, the return shape,
       and the exact error shape you forced. Scrub anything token-shaped before pasting.
  </how-to-verify>
  <resume-signal>
    Paste the raw per-step observations (or type "connector won't connect" / "MCP unavailable" and the
    error, if the round-trip could not complete).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the observed tool surface into the results doc and mcp-degradation.md</name>
  <files>spikes/mcp-round-trip-results.md, project-knowledge/mcp-degradation.md</files>
  <read_first>
    The human's pasted observations from the checkpoint; mcp-round-trip-protocol.md (recording-table
    headers); 01-RESEARCH.md §"Recording template for mcp-degradation.md" and §"Pitfall 2" (community
    list is a hypothesis only).
  </read_first>
  <action>
    Transcribe the human's pasted observations into spikes/mcp-round-trip-results.md as the dated raw
    record, then populate the "Observed tool surface" section of project-knowledge/mcp-degradation.md
    with one table row per observed tool — the observed tool name, whether it matched the community-list
    naming hypothesis, the parameters as observed, the return shape as observed, the error shape as
    observed, and the degrade behaviour if that call fails. Mark every row dated and observed-live. Note
    explicitly whether a favourites-equivalent tool exists (this settles whether seed-favourites.md's
    bootstrap role is temporary). Before saving either file, scan for and remove any token-shaped or
    credential-shaped string. If the round-trip could not complete, record that outcome instead: mark the
    tool surface UNVERIFIED with the blocking reason, and keep mcp-degradation.md's policy in force with
    tool names flagged as unconfirmed hypotheses — do not invent observed values.
  </action>
  <verify>
    <automated>test -f spikes/mcp-round-trip-results.md && grep -qi "observed\|unverified" project-knowledge/mcp-degradation.md && test $(grep -rEi "bearer |access_token|client_secret|authorization: " spikes/mcp-round-trip-results.md project-knowledge/mcp-degradation.md | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - spikes/mcp-round-trip-results.md holds the dated raw per-step observations (or a recorded blocking reason).
    - mcp-degradation.md's Observed tool surface section is populated with dated observed-live rows (or explicitly marked UNVERIFIED with the reason if the round-trip could not complete).
    - The favourites-tool question is answered (present / absent / unverified).
    - No token- or credential-shaped string appears in either file.
  </acceptance_criteria>
  <done>mcp-degradation.md's observed-tool-surface is filled from a dated live round-trip (or marked UNVERIFIED with the blocker), the favourites-tool question is answered, and no credential-shaped string is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human debugging notes → durable Project files | Round-trip observations could carry a token-shaped string into a committed doc |
| Rohlík MCP (experimental external service) → skills | Real tool surface is unknown until observed; write acknowledgements untrustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | mcp-round-trip-results.md / mcp-degradation.md | high | mitigate | Explicit credential-scan step in the checklist and in Task 3; automated grep gate rejects bearer/token/secret strings before save (CLAUDE.md §6, ASVS V2) |
| T-01-06 | Spoofing (fake success) | recorded degrade policy | high | mitigate | Round-trip empirically confirms success is only knowable via read-back, not the write return value (D-09) |
| T-01-07 | Tampering (drift from reality) | Observed tool surface table | medium | mitigate | Every row dated + observed-live; community tool list used only as a labelled hypothesis, never as ground truth (Pitfall 2) |
</threat_model>

<verification>
- The connector reads Connected and the eight docs are uploaded (human-confirmed at the checkpoint).
- mcp-degradation.md's Observed tool surface is filled from a dated live round-trip, or explicitly marked UNVERIFIED with a blocking reason.
- Credential-scan gate passes: no token/secret/bearer string in the results doc or mcp-degradation.md.
</verification>

<success_criteria>
- FOUND-04 is complete: the MCP-degradation policy defines fallback for every Rohlík MCP call AND its tool surface is grounded in observed behaviour (or the blocker is recorded, keeping the policy in force with unconfirmed tool names).
- ROADMAP success criterion 1's "validated against a live MCP round-trip" clause is satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shared-spine/01-02-SUMMARY.md` when done.
</output>
