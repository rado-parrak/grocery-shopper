---
phase: 02-quick-add-basket-review
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - skills/evals/quick-add-evals.md
  - skills/evals/basket-review-evals.md
  - skills/evals/uat-quick-add-basket-review.md
  - skills/README.md
autonomous: true
requirements:
  - UX-04
user_setup:
  - service: rohlik-mcp
    why: "Live end-to-end UAT needs the Rohlík MCP OAuth connector already set up at This-project scope on the shared Claude account"
    dashboard_config:
      - task: "Fill household-ruleset.md §A/§B and budget.md ⚠ FILL placeholders before the real shop"
        location: "project-knowledge/ files, hand-edited"

estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "'add milk and bananas' triggers quick-add (not basket-review) and 'what's in the basket' triggers basket-review (not quick-add) — proven by 3+ eval phrasings per skill including cross-skill near-misses"
    - "The zip-and-upload-to-claude.ai flow is documented and it is stated these skills are NOT for .claude/skills/"
    - "A household member can run the real end-to-end quick-add on a phone and reach the shared basket in ≤3 turns with correct products and a Czech audit [UX-04]"
  artifacts:
    - path: "skills/evals/quick-add-evals.md"
      provides: "3+ trigger phrasings for quick-add incl. cross-skill near-misses"
    - path: "skills/evals/basket-review-evals.md"
      provides: "3+ trigger phrasings for basket-review incl. cross-skill near-misses"
    - path: "skills/evals/uat-quick-add-basket-review.md"
      provides: "The human UAT procedure measuring the ≤3-turn path on a real phone"
    - path: "skills/README.md"
      provides: "Packaging/upload guide — zip to claude.ai Settings → Features; not for .claude/skills/"
  key_links:
    - from: "skills/evals/quick-add-evals.md"
      to: "skills/quick-add/SKILL.md"
      via: "eval phrasings target the authored description"
      pattern: "quick-add"
    - from: "skills/evals/basket-review-evals.md"
      to: "skills/basket-review/SKILL.md"
      via: "eval phrasings target the authored description"
      pattern: "basket-review"
---

<objective>
Validate the two skills and package them: author a per-skill trigger eval set (3+ phrasings each, including cross-skill near-misses), a README documenting the zip-and-upload-to-claude.ai flow, and a human UAT procedure that measures the real ≤3-turn quick-add path on a phone.

Purpose: The eval sets guard against the top multi-skill risk (cross-firing between quick-add and basket-review); the UAT is the only place the ≤3-turn turn-economy requirement (UX-04) is truly measurable, against the real shared basket.
Output: `skills/evals/*.md` + `skills/README.md`, plus a `<verify><human-check>` UAT (per end-of-phase human-verify mode).
</objective>

<execution_context>
@/home/user/grocery-shopper/.claude/gsd-core/workflows/execute-plan.md
@/home/user/grocery-shopper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-add-basket-review/02-CONTEXT.md

@skills/quick-add/SKILL.md
@skills/basket-review/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger eval sets — 3+ phrasings per skill, cross-skill near-misses</name>
  <files>skills/evals/quick-add-evals.md, skills/evals/basket-review-evals.md</files>
  <read_first>
    - skills/quick-add/SKILL.md (the authored description under test)
    - skills/basket-review/SKILL.md (the authored description under test)
    - .planning/phases/02-quick-add-basket-review/02-CONTEXT.md (D-03 disjoint trigger vocabulary, §Specifics eval requirement)
  </read_first>
  <action>
    Author two markdown eval files (test cases only — there is no runner). skills/evals/quick-add-evals.md lists at least three phrasings that MUST trigger quick-add (e.g. English "add milk and bananas", "we need eggs"; Czech "přidej máslo") and at least two cross-skill near-miss phrasings that must trigger a DIFFERENT skill and NOT quick-add (e.g. "what's in the basket" → basket-review; a future "restock the usual" → not quick-add). skills/evals/basket-review-evals.md mirrors this: at least three phrasings that MUST trigger basket-review (e.g. "what's in the basket", "co je v košíku", "check the cart / running total") and at least two near-misses that must NOT (e.g. "add milk and bananas" → quick-add).

    For each case give: the input phrasing, the expected skill, and the expected NOT-triggered skill(s). Include a short header stating the eval intent: confirm "add milk and bananas" fires quick-add and not basket-review, and vice-versa, per research Pattern 1 and D-03.
  </action>
  <acceptance_criteria>
    - skills/evals/quick-add-evals.md contains ≥3 must-trigger phrasings (incl. one Czech) and ≥2 cross-skill near-miss cases naming basket-review.
    - skills/evals/basket-review-evals.md contains ≥3 must-trigger phrasings (incl. one Czech) and ≥2 near-miss cases naming quick-add.
    - Each file states expected-trigger and expected-NOT-trigger per case.
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/evals/quick-add-evals.md && test -f skills/evals/basket-review-evals.md && grep -qi "add milk and bananas" skills/evals/quick-add-evals.md && grep -q "přidej" skills/evals/quick-add-evals.md && grep -qi "basket-review" skills/evals/quick-add-evals.md && grep -q "košíku" skills/evals/basket-review-evals.md && grep -qi "quick-add" skills/evals/basket-review-evals.md && echo EVALS_OK</automated>
  </verify>
  <done>Both eval files exist with ≥3 must-trigger phrasings (incl. Czech) and ≥2 cross-skill near-misses each, guarding against quick-add ↔ basket-review cross-firing.</done>
</task>

<task type="auto">
  <name>Task 2: Packaging README — zip-and-upload flow, not for .claude/skills/</name>
  <files>skills/README.md</files>
  <read_first>
    - .planning/phases/02-quick-add-basket-review/02-CONTEXT.md (D-01, D-02 packaging/location decisions)
    - .claude/CLAUDE.md (Version/Surface Compatibility Notes — claude.ai zip upload vs Claude Code .claude/skills/)
  </read_first>
  <action>
    Author skills/README.md explaining the authoring→runtime flow per D-01/D-02: these SKILL.md sources are version-controlled here, then zipped per skill and uploaded to the claude.ai Project via Settings → Features; the eight project-knowledge/ contracts are uploaded separately as Project Knowledge files; the Rohlík MCP connector is granted at This-project scope. State explicitly and prominently that these skills are NOT to be placed in .claude/skills/ — that path is Claude Code's own execution surface (occupied by GSD) and is the wrong runtime; the claude.ai upload is the only intended runtime. Note OAuth-only: never paste a token or secret into any skill file. Keep it short and practical.
  </action>
  <acceptance_criteria>
    - skills/README.md documents the zip + Settings → Features upload flow and the separate Project-Knowledge upload of the contracts.
    - It states explicitly the skills are NOT for .claude/skills/.
    - It notes OAuth-only / no credentials in skill files.
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/README.md && grep -Eiq "Settings . Features|Settings → Features|zip" skills/README.md && grep -q ".claude/skills/" skills/README.md && grep -Eiq "OAuth|credential|token" skills/README.md && echo README_OK</automated>
  </verify>
  <done>skills/README.md documents the claude.ai zip-upload flow, the not-for-.claude/skills/ rule, and the OAuth-only credential rule.</done>
</task>

<task type="auto">
  <name>Task 3: Author the human UAT procedure and run it (≤3-turn measurement)</name>
  <files>skills/evals/uat-quick-add-basket-review.md</files>
  <read_first>
    - skills/quick-add/SKILL.md
    - skills/basket-review/SKILL.md
    - .planning/phases/02-quick-add-basket-review/02-CONTEXT.md (§Deferred — live UAT needs a phone + ⚠ HOUSEHOLD data filled)
  </read_first>
  <precondition>The Rohlík MCP OAuth connector is set up (This-project scope) on the shared Claude account, and household-ruleset.md §A/§B + budget.md ⚠ FILL placeholders are filled — otherwise the real shop cannot run.</precondition>
  <action>
    Author skills/evals/uat-quick-add-basket-review.md: a step-by-step manual UAT the household runs on a phone. It must specify, as an explicit checklist: open a fresh chat in the Project; type "add milk and bananas" (and separately a Czech variant "přidej mléko a banány"); confirm quick-add triggers, a confirmation artifact renders with tickboxes + quantity steppers + running total + budget lines, the reply is phone-terse; confirm in the next turn; verify the correct Czech-named products land in the shared basket via a read-back, a Czech audit line is emitted, and the whole path completed in ≤3 turns (UX-04); then type "what's in the basket" and confirm basket-review reads the live cart with total + budget state and hands off to manual checkout. Include a pass/fail line for each check and a note that checkout stays manual (no forbidden tool is ever called).

    After authoring the procedure, this task's completion is gated on the human running it — captured by the human-check verify below (per end-of-phase human-verify mode).
  </action>
  <acceptance_criteria>
    - skills/evals/uat-quick-add-basket-review.md exists with an ordered checklist covering: quick-add trigger, confirmation artifact, ≤3-turn completion, correct Czech products in basket, Czech audit, basket-review read-back + budget state + manual-checkout handoff.
    - It states the precondition (connector set up + ⚠ HOUSEHOLD data filled) and includes bilingual test phrases.
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/evals/uat-quick-add-basket-review.md && grep -qi "add milk and bananas" skills/evals/uat-quick-add-basket-review.md && grep -Eiq "3 turn|≤3|three turn" skills/evals/uat-quick-add-basket-review.md && grep -q "košík" skills/evals/uat-quick-add-basket-review.md && grep -Eiq "manual|checkout" skills/evals/uat-quick-add-basket-review.md && echo UAT_DOC_OK</automated>
    <human-check>On a phone signed into the shared Claude Project (connector set up; ⚠ HOUSEHOLD ruleset/budget filled): run skills/evals/uat-quick-add-basket-review.md end to end. Confirm "add milk and bananas" reaches the shared basket with the right Czech products, a Czech audit line, and a confirmation artifact — in ≤3 turns — and that "what's in the basket" reads it back with budget state and hands off to manual checkout. Report pass/fail per checklist item.</human-check>
  </verify>
  <done>The UAT procedure exists and a household member has run it on a real phone, confirming the ≤3-turn quick-add path (UX-04) and the basket-review read-back against the real shared basket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human ↔ live shared basket | UAT performs real cart mutations on the shared Rohlík account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Elevation | UAT accidentally exercises a checkout/payment path | high | mitigate | UAT checklist explicitly ends at manual handoff; no forbidden tool is invoked; checkout stays in the Rohlík app |
| T-02-11 | Information disclosure | OAuth token pasted into an eval/UAT/README file "to get it working" | medium | mitigate | OAuth-only rule restated in README; token-shaped-string scan at phase verify |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — pure markdown authoring |
</threat_model>

<verification>
- Task 1 and Task 2 automated grep gates pass; Task 3 doc gate passes and the human-check UAT is recorded pass/fail.
- No OAuth token string appears in any skills/ file.
</verification>

<success_criteria>
- Cross-skill trigger separation (D-03) is demonstrated by the eval sets.
- The ≤3-turn quick-add path (UX-04) is measured on a real phone via the UAT.
- Packaging is documented so the household can zip-and-upload without guessing.
</success_criteria>

<output>
Create `.planning/phases/02-quick-add-basket-review/02-03-SUMMARY.md` when done.
</output>
