---
phase: 02-quick-add-basket-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/basket-review/SKILL.md
autonomous: true
requirements:
  - REVW-01
  - REVW-02
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "'what's in the basket' / 'co je v košíku' / 'check the cart' triggers basket-review (not quick-add), reads the live cart via Rohlik:get_cart, and lists items with a running total [REVW-01]"
    - "basket-review reports budget-threshold state (soft/hard per budget.md) and hands off to the manual Rohlík-app checkout, never calling any checkout/order/payment tool [REVW-02]"
    - "basket-review degrades gracefully if Rohlik:get_cart fails and never fabricates cart contents [DEGR by reference]"
  artifacts:
    - path: "skills/basket-review/SKILL.md"
      provides: "The basket-review skill source — frontmatter name+description + thin-orchestrator body"
      contains: "Rohlik:get_cart"
  key_links:
    - from: "skills/basket-review/SKILL.md"
      to: "project-knowledge/mcp-degradation.md"
      via: "get_cart + degradation branch + Forbidden-tools prohibition"
      pattern: "mcp-degradation\\.md"
    - from: "skills/basket-review/SKILL.md"
      to: "project-knowledge/budget.md"
      via: "soft/hard threshold state read-out"
      pattern: "budget\\.md"
    - from: "skills/basket-review/SKILL.md"
      to: "project-knowledge/audit-format.md"
      via: "Czech product names + running-total line"
      pattern: "audit-format\\.md"
---

<objective>
Author `skills/basket-review/SKILL.md`: the read-only basket-review Claude Skill that answers "what's in the basket" / "co je v košíku" by reading the live shared cart, showing items with a running total and budget-threshold state, then handing off to the manual Rohlík-app checkout.

**User story:** As a household member on my phone, I want to ask what's in our shared basket and see the total and how close we are to our budget, so that I can decide to check out myself in the Rohlík app.

Purpose: The read-back half of the vertical slice. It reuses the same spine (get_cart, budget thresholds, Czech product names) without ever mutating the cart.
Output: A thin-orchestrator SKILL.md that references the spine contracts by name, uses only allowed read/discovery tools, and never checks out.
</objective>

<execution_context>
@/home/user/grocery-shopper/.claude/gsd-core/workflows/execute-plan.md
@/home/user/grocery-shopper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-add-basket-review/02-CONTEXT.md

@project-knowledge/mcp-degradation.md
@project-knowledge/budget.md
@project-knowledge/audit-format.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author basket-review SKILL.md — trigger + live cart read + total</name>
  <files>skills/basket-review/SKILL.md</files>
  <read_first>
    - project-knowledge/mcp-degradation.md (Rohlik:get_cart return shape, degradation branch, Forbidden-tools section)
    - project-knowledge/audit-format.md (Czech product-name Language Rule, running-total line "Celkem v košíku")
    - .planning/phases/02-quick-add-basket-review/02-CONTEXT.md (D-03 disjoint trigger vocabulary, D-08 bilingual/mobile)
  </read_first>
  <action>
    Create skills/basket-review/SKILL.md with valid Claude-Skill YAML frontmatter (`name`, `description` only required). Set name to the kebab-case `basket-review`. Write a third-person "what + when" description ≤1024 chars with concrete Czech AND English trigger phrases scoped strictly to *inspecting the cart* per D-03 — English "what's in the basket", "check the cart", "running total"; Czech "co je v košíku" — and explicitly NOT the "add X" phrasing that belongs to quick-add, so the two skills never cross-fire.

    In the body, author a thin-orchestrator read-only procedure: (1) read the live shared cart with the fully-qualified `Rohlik:get_cart` tool; (2) list the items with quantities and per-line prices, rendering product names in Czech verbatim from the catalogue per audit-format.md's Language Rule (referenced by name), phone-terse, single-column, no wide tables; (3) show the running total using the audit-format.md running-total line convention. Keep the body short and well under ~500 lines. Do not re-derive the audit format — reference it by name.
  </action>
  <acceptance_criteria>
    - skills/basket-review/SKILL.md exists with frontmatter keys `name` and `description`; `name` is `basket-review`.
    - The description contains a Czech trigger phrase (e.g. "košíku") and an English trigger phrase (e.g. "what's in the basket" / "check the cart"), and does not contain "add milk"-style quick-add trigger vocabulary.
    - The body uses Rohlik:get_cart and references audit-format.md for Czech product names + the running-total line.
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/basket-review/SKILL.md && grep -q "^name: basket-review" skills/basket-review/SKILL.md && grep -q "košíku" skills/basket-review/SKILL.md && grep -Eiq "what's in the basket|check the cart" skills/basket-review/SKILL.md && grep -q "Rohlik:get_cart" skills/basket-review/SKILL.md && grep -q "audit-format.md" skills/basket-review/SKILL.md && ! grep -qi "add milk and bananas" skills/basket-review/SKILL.md && echo REVW_READ_OK</automated>
  </verify>
  <done>basket-review triggers on cart-inspection phrasing (CZ+EN, disjoint from quick-add), reads the live cart via Rohlik:get_cart, and lists items with a Czech-named running total.</done>
</task>

<task type="auto">
  <name>Task 2: Budget-threshold state, degradation, and manual-checkout handoff</name>
  <files>skills/basket-review/SKILL.md</files>
  <read_first>
    - project-knowledge/budget.md (soft threshold, hard cap, projected-total semantics)
    - project-knowledge/mcp-degradation.md (degrade-to-list branch, Forbidden-tools prohibition)
    - skills/basket-review/SKILL.md (Task 1 body)
  </read_first>
  <action>
    Extend skills/basket-review/SKILL.md, each rule deferring to its spine doc by name. Budget state (defer to budget.md): after showing the total, report where the basket sits against the soft threshold and hard cap — plainly note if it is at/over the soft threshold, and if it is at/over the hard cap (REVW-02). This is a read-only report; basket-review never writes and never blocks (blocking is quick-add's job before a write).

    Handoff: state that checkout is a manual human step performed directly in the Rohlík app, and that basket-review must never call any tool in mcp-degradation.md's "Forbidden tools — never call" section — reference that section by name rather than listing the tool names, to avoid duplicating the single source of truth.

    Degradation (defer to mcp-degradation.md): if Rohlik:get_cart fails, times out, or returns an unexpected shape, say plainly that the Rohlík connection is not working right now rather than inventing or guessing cart contents — never fabricate a basket read.
  </action>
  <acceptance_criteria>
    - SKILL.md references budget.md and reports soft-threshold and hard-cap state as a read-only report.
    - SKILL.md states checkout is a manual step in the Rohlík app and defers to mcp-degradation.md's Forbidden-tools section for the never-call prohibition.
    - SKILL.md references mcp-degradation.md for the degrade branch and states it never fabricates cart contents on failure.
    - No forbidden checkout/order/payment tool literal appears in the file.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "budget.md" skills/basket-review/SKILL.md && grep -Eiq "soft threshold|soft" skills/basket-review/SKILL.md && grep -Eiq "hard cap" skills/basket-review/SKILL.md && grep -q "mcp-degradation.md" skills/basket-review/SKILL.md && grep -Eiq "manual|Rohlík app|checkout" skills/basket-review/SKILL.md && ! grep -Eiq "submit_checkout|reserve_timeslot|cancel_order|submit_claim|repeat_order|update_payment_method_checkout" skills/basket-review/SKILL.md && echo REVW_BUDGET_OK</automated>
  </verify>
  <done>basket-review reports budget-threshold state, hands off to manual Rohlík-app checkout without calling any forbidden tool, and degrades honestly if the cart read fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → skill | Free-text cart-inspection request crosses into the read flow |
| skill → Rohlík MCP | Read-only get_cart call to an experimental connector that also exposes checkout/payment tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation | basket-review → forbidden checkout/payment tools during "handoff" | high | mitigate | Handoff is text-only; SKILL defers to mcp-degradation.md Forbidden-tools; negative-grep gate (Task 2 verify) |
| T-02-08 | Spoofing/Repudiation | Fabricated cart contents when get_cart fails | medium | mitigate | Degradation branch states the connection is down rather than inventing contents (Task 2) |
| T-02-09 | Information disclosure | OAuth token / secret pasted into SKILL.md | medium | mitigate | OAuth-only authoring rule; token-shaped-string scan at phase verify |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — pure markdown authoring |
</threat_model>

<verification>
- Both task `<verify>` grep gates pass.
- SKILL.md body is under ~500 lines, references the spine docs by name, and never re-derives the audit format.
- No forbidden checkout/order/payment tool literal and no OAuth token string appears in skills/basket-review/.
</verification>

<success_criteria>
- Phase-2 success criterion 5's basket-review half (REVW-01, REVW-02) is satisfiable by this skill's authored instructions.
- Trigger vocabulary is disjoint from quick-add per D-03 (validated by the eval set in 02-03).
</success_criteria>

<output>
Create `.planning/phases/02-quick-add-basket-review/02-02-SUMMARY.md` when done.
</output>
